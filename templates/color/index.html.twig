{% extends 'admin-base.html.twig' %}

{% block title %}Color index{% endblock %}

{% block body %}
<div class="container">
    <h1>Color index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ColorPreview</th>
                <th>ColorID</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for color in colors %}
            <tr>
                <td>{{ color.id }}</td>
                <td>{{ color.colorPreview }}</td>
                <td>{{ color.colorID }}</td>
                <td>{{ color.name }}</td>
                <td>
                    <a href="{{ path('color_show', {'id': color.id}) }}">show</a>
                    <a href="{{ path('color_edit', {'id': color.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('color_new') }}">Create new</a>
</div>
{% endblock %}
