<form method="post" action="{{ path('etherystal_delete', {'id': etherystal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etherystal.id) }}">
    <button class="btn">Delete</button>
</form>
