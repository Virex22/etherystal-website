<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
            <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
                <img src="{{asset('images/Etherystal_Logo.png')}}" alt="Etherystal_Logo" width="50" height="50" class="ml-3">
                <span class="fs-2"> Etherystal</span>
            </a>

            <ul class="nav nav-pills">
                <li class="nav-item"><a href="{{path("home")}}" class="nav-link" aria-current="page">Home</a></li>
                <li class="nav-item"><a href="{{path("item")}}" class="nav-link">Items</a></li>
                <li class="nav-item"><a href="{{path("price_negotiation")}}" class="nav-link">Price negotiation</a></li>
                <li class="nav-item"><a href="{{path("info")}}" class="nav-link">Opportunity and information</a></li>
            </ul>
        </header>
        {% block body %}{% endblock %}
        <hr class="featurette-divider">
        <footer class="container my-5">
            <p class="float-end"><a href="#">Go back up</a></p>
            <p>© 2021–2022 Etherystal.<a href="{{path("mention_legal")}}">legal notice.</a></p>
        </footer>
    </body>
</html>
