{% extends 'admin-base.html.twig' %}

{% block title %}Rarety index{% endblock %}

{% block body %}
<div class="container">
    <h1>Rarety index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rarety in rareties %}
            <tr>
                <td>{{ rarety.id }}</td>
                <td>{{ rarety.name }}</td>
                <td>{{ rarety.description }}</td>
                <td>
                    <a href="{{ path('rarety_show', {'id': rarety.id}) }}">show</a>
                    <a href="{{ path('rarety_edit', {'id': rarety.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('rarety_new') }}">Create new</a>
    </div>
{% endblock %}
